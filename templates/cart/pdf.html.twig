<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facture du Panier</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Custom styles */
        body {
            background-color: #f4f6f9;
        }

        .container-fluid {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 50px 0;
        }

        h1 {
            color: #003366;
            font-weight: bold;
        }

        h2 {
            color: #666;
        }

        .invoice {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            border: 2px solid #003366; /* Custom border for the invoice */
        }

        .header {
            background-color: #003366;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
            margin-bottom: 30px;
        }

        .header h1 {
            margin: 0;
            color: #fff;
        }

        .header h2 {
            margin: 0;
            color: #f9f9f9;
        }

        .header p {
            margin: 10px 0;
            color: #f9f9f9;
        }

        .table th, .table td {
            text-align: left;
            padding: 10px;
            border: 1px solid #ddd; /* Border for table cells */
        }

        .table th {
            background-color: #003366;
            color: white;
        }

        .table td {
            background-color: #f9f9f9;
        }

        .total {
            text-align: right;
            font-weight: bold;
            margin-top: 20px;
            font-size: 16px;
            color: #003366;
        }

        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 2px solid #003366;
            text-align: center;
            color: #555;
            font-size: 12px;
        }

        .contact-section {
            background-color: #003366;
            color: white;
            padding: 30px;
            margin-top: 50px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }

        .contact-section h4 {
            color: #fff;
        }

        .contact-section p {
            font-size: 16px;
        }

        .contact-section .btn-primary {
            background-color: #fff;
            color: #003366;
            border-color: #fff;
        }

        .contact-section .btn-primary:hover {
            background-color: #003366;
            color: #fff;
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <div class="header">
        <!-- BrightMind Header -->
    <div class="container-fluid">
        <div>
            <h1>Bright Mind</h1>
            <h2>SYSTÈME INTELLIGENT DE GESTION HOSPITALIER</h2>
            <h3>Créé par ActUp</h3>
        </div>
    

    <div class="invoice">
        <!-- En-tête de la facture -->
        <div class="header">
            <h3>Facture du Panier</h3>
            <p>Date : {{ "now"|date("d/m/Y") }}</p>
            <p>Numéro de facture : #{{ "now"|date("YmdHis") }}</p>
        </div>

        <!-- Informations du client et du vendeur -->
        <div class="info">
            <!-- Tu peux ajouter ici les informations client et vendeur -->
        </div>

        <!-- Tableau des articles -->
        <table class="table">
            <thead>
                <tr>
                    <th>Nom du Médicament</th>
                    <th>Prix Unitaire (TND)</th>
                    <th>Quantité</th>
                    <th>Total (TND)</th>
                </tr>
            </thead>
            <tbody>
                {% for medicament in cart %}
                    <tr>
                        <td>{{ medicament.NomMedicament }}</td>
                        <td>{{ medicament.Prix|number_format(2, ',', ' ') }}</td>
                        <td>{{ medicament.quantity }}</td>
                        <td>{{ medicament.subtotal|number_format(2, ',', ' ') }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <!-- Montant total -->
        <p class="total">Montant Total : <strong>{{ total|number_format(2, ',', ' ') }} TND</strong></p>

        <!-- Pied de page -->
        <div class="footer">
            <p>Merci pour votre achat !</p>
            <p>Pour toute question, contactez-nous à contact@pharmaciexyz.tn ou au +216 71 123 456.</p>
            <p>© 2023 Pharmacie XYZ. Tous droits réservés.</p>
        </div>
    </div>

    <!-- Contact Us Section -->
    <div class="contact-section">
        <h4>Contactez-nous</h4>
        <p>Adresse: 123 Rue de l'Innovation, Tunis, Tunisie</p>
        <p>Téléphone: +216 70 000 000</p>
        <p>Email: contact@brightmind.com</p>
        <div class="mt-3">
            <a href="mailto:contact@brightmind.com" class="btn btn-primary">Envoyer un Email</a>
        </div>
    </div>

    <!-- Bootstrap JS (optional, for any further interactivity) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
